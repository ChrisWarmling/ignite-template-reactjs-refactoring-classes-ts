export interface FoodItens {
    id: number;
    name: string;
    description: string;
    price: string;
    available: boolean;
    image: string;
}

export interface DataItens {
    name: string,
    image: string,
    price: number,
    description: string,
  }